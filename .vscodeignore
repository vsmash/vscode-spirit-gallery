.vscode/**
.git/**
.gitignore
.gitattributes
**/*.map
node_modules/**

# Tooling / local config — must never ship to the marketplace
.claude/**
CLAUDE.md
.env.maiass*
devlog.csv
*.vsix

# Screenshot sources — referenced by URL in README, not bundled
samples/**
images/**
